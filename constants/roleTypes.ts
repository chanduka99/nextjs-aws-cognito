// Role types corresponds to the groups made in aws cognito
export enum RoleType {
  ADMIN = "Admin",
  STANDARD_USER = "Standard User",
}

export enum CognitoGroupType {
  ADMIN = "Admins",
  STANDARD_USER = "StandardUsers",
}
